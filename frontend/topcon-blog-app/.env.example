# =============================================================================
# TopconBlog Frontend - Variáveis de Ambiente (Exemplo)
# =============================================================================
# Copie este arquivo para .env e configure os valores

# URL base da API backend (sem /api no final)
VITE_API_BASE_URL=http://localhost:8080

# Chave da API do TinyMCE (editor de texto rico)
# Obtenha sua chave em: https://www.tiny.cloud/
VITE_TINYMCE_API_KEY=sua_chave_aqui
